## R CMD check results

0 errors | 0 warnings | 0 notes

This update allows `inshiny` widgets to be used dynamically with Shiny's 
`uiOutput()` and `renderUI()`, and with Shiny modules. Also fixes some minor
display and behaviour bugs.
